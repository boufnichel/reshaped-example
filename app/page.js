'use client';

import { View, Button } from 'reshaped';

export default function Page() {
  return (
    <View padding={2}>
      <Button href="https://reshaped.so" color="primary">Click me</Button>
    </View>
  );
}
